{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:offline": "VITE_OFFLINE_MODE=1 vite",
    "build": "tsc && vite build",
    "build:offline": "VITE_OFFLINE_MODE=1 tsc && VITE_OFFLINE_MODE=1 vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@freenetorg/freenet-stdlib": "^0.2.0"
  },
  "devDependencies": {
    "sass": "^1.87.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.1.0"
  }
}
