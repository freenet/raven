[contract]
type = "webapp"
lang = "rust"

[webapp.state-sources]
source_dirs = ["dist"]

# No build-time contract dependency: the app instantiates parameterized shards
# (user/thread/inbox) at runtime by PUTting their bundled WASM. See ADR-0001.
