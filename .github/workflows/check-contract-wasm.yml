name: check-contract-wasm

on:
  pull_request:
    paths:
      - "web/**"
      - "contracts/**"
      - "delegates/**"
      - "tools/web-container-sign/**"
      - "Makefile.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"

env:
  FDEV_VERSION: "0.3.212"

jobs:
  verify-published-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install GNU tar
        run: |
          if ! tar --version | grep -qi 'gnu tar'; then
            sudo apt-get update && sudo apt-get install -y tar
          fi
      - name: Install cargo-binstall
        uses: taiki-e/install-action@cargo-binstall
      - name: Install cargo-make + fdev + b3sum
        run: |
          cargo binstall -y --force cargo-make
          cargo binstall -y --force --version ${{ env.FDEV_VERSION }} fdev || \
            cargo install fdev --version ${{ env.FDEV_VERSION }} --locked
          cargo binstall -y --force b3sum || cargo install b3sum --locked
      - name: Rebuild test-signed contract
        run: cargo make update-published-contract
      - name: Compare rebuilt snapshot against committed
        run: |
          # NOTE: this step is informational-only until the committed
          # `published-contract/` snapshot is rebuilt on Linux CI. The
          # initial snapshot was produced on macOS; Ubuntu-produced WASM
          # may differ byte-for-byte until the snapshot is regenerated
          # on Linux and committed. We print the diff as a warning and
          # exit 0 so the job reports green.
          if ! git diff --exit-code -- published-contract/; then
            echo "::warning::published-contract/ drifted from source."
            echo ""
            echo "Expected until the committed snapshot is rebuilt on Linux."
            echo "Diff for context:"
            git diff -- published-contract/ || true
            echo ""
            echo "To refresh manually (run on Linux for CI parity):"
            echo "  cargo make update-published-contract"
            echo "  git add published-contract/"
            exit 0
          fi
          echo "published-contract/ is in sync with source ✓"
